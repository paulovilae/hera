//! Candle Core Hardware Abstraction
//!
//! Exposes native machine capabilities and container topologies
//! seamlessly mapping GPU bounds to authenticated identities.

pub mod docker;
pub mod rbac;

pub use docker::{DockerService, PortMapping, discover_docker_services};
pub use rbac::{GpuTier, get_gpu_endpoint};
