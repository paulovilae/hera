#!/usr/bin/env bash
set -euo pipefail

BASE_URL="${HERA_CLAUDE_CODE_BASE_URL:-http://127.0.0.1:3002}"
API_KEY="${HERA_CLAUDE_CODE_API_KEY:-dummy}"

export ANTHROPIC_BASE_URL="${BASE_URL}"
export ANTHROPIC_API_KEY="${API_KEY}"

exec npx -y @anthropic-ai/claude-code "$@"
